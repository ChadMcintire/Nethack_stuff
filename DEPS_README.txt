Install NLE via ".[dev]" as described in the README
Commands from README if you're lazy:
	$ git clone https://github.com/facebookresearch/nle --recursive
	$ pip install -e ".[dev]"
	$ pre-commit install

Extra deps needed to run agent.plot

Linux:
	- gnuplot
	
Python:
	- pandas
	- <unknown - I forgot>
